tdss - tokyo dystopia snap search
=================================

Summary
-------
Simple full text search indexer and web ui for html files with
tokyodystopia and snap web framework.

Commands overview
-----------------
For detailed argument, run `tdss --help`.

* `tdss index`
    * Index html files under given directory, and write indexed data.

* `tdss serve`
    * Run server program with given template path and db path.
